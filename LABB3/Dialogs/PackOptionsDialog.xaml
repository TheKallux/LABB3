<Window x:Class="LABB3.Dialogs.PackOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pack Options" Height="400" Width="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Pack Name:" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="PackNameTextBox" Height="40" Margin="0,0,0,20" Padding="10"/>

            <TextBlock Text="Difficulty:" FontSize="14" Margin="0,0,0,5"/>
            <ComboBox x:Name="DifficultyComboBox" Height="40" Margin="0,0,0,20">
                <ComboBoxItem>Easy</ComboBoxItem>
                <ComboBoxItem>Medium</ComboBoxItem>
                <ComboBoxItem>Hard</ComboBoxItem>
            </ComboBox>

            <TextBlock Text="Time per question:" FontSize="14" Margin="0,0,0,5"/>
            <Slider x:Name="TimeSlider" 
                    Minimum="5" 
                    Maximum="60" 
                    Value="30"
                    TickPlacement="BottomRight"
                    TickFrequency="5"
                    IsSnapToTickEnabled="True"
                    Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding ElementName=TimeSlider,
                                Path=Value, StringFormat='Time: {0} seconds'}" 
                    Foreground="Gray" 
                    FontSize="12"
                    Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Save" Width="100" Height="40" Margin="0,0,10,0" Click="SaveButton_Click"/>
                <Button Content="Cancel" Width="100" Height="40" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>